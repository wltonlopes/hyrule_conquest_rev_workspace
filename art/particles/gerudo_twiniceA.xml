<?xml version="1.0" encoding="utf-8"?>
<particles>

    <texture>art/textures/particles/gerudo_twiniceA.dds</texture>
    <blend mode="add"/>

    <constant name="emissionrate" value="17.0"/>
    <uniform name="lifetime" min="2.0" max="4.0"/>

    <constant name="position.y" value="0.0"/>

    <uniform name="angle" min="-1.5" max="1.5"/>

    <uniform name="velocity.x" min="-0.0" max="0.5"/>
    <uniform name="velocity.y" min="0.2" max="0.5"/>
    <uniform name="velocity.z" min="-0.5" max="0.5"/>
    <uniform name="velocity.angle" min="-0.5" max="0.5"/>

    <uniform name="size" min="0.5" max="1.5"/>

    <uniform name="color.r" min="0.8" max="1.0"/>
    <copy name="color.g" from="color.r"/>
    <copy name="color.b" from="color.r"/>

</particles>
