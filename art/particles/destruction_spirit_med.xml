<?xml version="1.0" encoding="utf-8"?>
<particles>

    <texture>art/textures/particles/gerudo_spirittrail.dds</texture>
    <blend mode="over"/>

    <constant name="emissionrate" value="20.0"/>
    <uniform name="lifetime" min="1.0" max="2.0"/>

    <constant name="position.y" value="4.0"/>

    <uniform name="angle" min="-3.14" max="3.14"/>

    <uniform name="velocity.x" min="-10.0" max="10.0"/>
    <uniform name="velocity.y" min="1.5" max="2.0"/>
    <uniform name="velocity.z" min="-10.0" max="10.0"/>
    <uniform name="velocity.angle" min="-21.5" max="21.5"/>

    <uniform name="size" min="2.0" max="7.5"/>

    <force y="-14.15"/>

</particles>
