<?xml version="1.0" encoding="utf-8"?>
<particles>

    <texture>art/textures/particles/water_splash.png</texture>
    <blend mode="over"/>

    <constant name="emissionrate" value="50.0"/>
    <uniform name="lifetime" min="2.0" max="33.0"/>

	<uniform name="position.x" min="-30.0" max="30.0"/>
    <uniform name="position.y" min="0.0" max="5.0"/>
	<uniform name="position.z" min="-30.0" max="30.0"/>

    <uniform name="angle" min="-3.14" max="3.14"/>

    <uniform name="velocity.x" min="-1.3" max="1.3"/>
    <uniform name="velocity.y" min="0.1" max="0.2"/>
    <uniform name="velocity.z" min="-1.5" max="1.5"/>
    <uniform name="velocity.angle" min="-0.1" max="0.1"/>

    <uniform name="size" min="2.5" max="17.5"/>

    <force y="-0.05"/>


</particles>
