<?xml version="1.0" encoding="utf-8"?>
<particles>

    <texture>art/textures/particles/beesA.png</texture>
    <blend mode="over"/>
	
    <constant name="emissionrate" value="55.0"/>
    <uniform name="lifetime" min="0.5" max="3.0"/>

	<uniform name="position.x" min="-3.5" max="3.5"/>
	<uniform name="position.y" min="-0.5" max="0.5"/>
    <uniform name="position.z" min="-3.5" max="3.5"/>

    <uniform name="angle" min="-7.3" max="7.3"/>

    <uniform name="velocity.x" min="-2.1" max="2.1"/>
    <uniform name="velocity.y" min="2.0" max="2.1"/>
    <uniform name="velocity.z" min="-2.1" max="2.1"/>
    <uniform name="velocity.angle" min="-2.1" max="2.1"/>

    <uniform name="size" min="2.5" max="4.7"/>
	
    <uniform name="color.b" min="9.8" max="10.0"/>
    <copy name="color.g" from="color.b"/>
    <copy name="color.r" from="color.b"/>

</particles>
