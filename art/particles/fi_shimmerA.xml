<?xml version="1.0" encoding="utf-8"?>
<particles>

    <texture>art/textures/particles/fi_shimmerA.dds</texture>
    <blend mode="add"/>

    <constant name="emissionrate" value="50.0"/>
    <uniform name="lifetime" min="1.5" max="30.0"/>

	<uniform name="position.x" min="-3.0" max="3.0"/>
	<uniform name="position.y" min="-1" max="0.0"/>
    <uniform name="position.z" min="-3.0" max="3.0"/>

    <uniform name="angle" value="0"/>

    <uniform name="velocity.x" min="-0.1" max="0.1"/>
    <uniform name="velocity.y" min="0.1" max="7.5"/>
    <uniform name="velocity.z" min="-0.1" max="0.1"/>
    <uniform name="velocity.angle" value="0"/>

    <uniform name="size" min="3.4" max="21.0"/>

</particles>
